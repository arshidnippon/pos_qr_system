<div class="container my-5">
  <!-- Back to Dashboard -->
  <div class="mb-3">
    <%= link_to "← Back to Dashboard", root_path,
      class: "btn btn-outline-secondary btn-sm" %>
  </div>

  <!-- Page Title -->
  <h1 class="h4 text-primary fw-bold mb-4">Sales Reports</h1>

  <!-- Daily Sales -->
  <div class="mb-5">
    <h2 class="h6 fw-semibold mb-2">Daily Sales</h2>
    <div class="border rounded p-3 bg-white shadow-sm">
      <%= line_chart @daily_sales %>
    </div>
  </div>

  <!-- Monthly Sales -->
  <div class="mb-5">
    <h2 class="h6 fw-semibold mb-2">Monthly Sales</h2>
    <div class="border rounded p-3 bg-white shadow-sm">
      <%= column_chart @monthly_sales %>
    </div>
  </div>

  <!-- Yearly Sales -->
  <div class="mb-3">
    <h2 class="h6 fw-semibold mb-2">Yearly Sales</h2>
    <div class="border rounded p-3 bg-white shadow-sm">
      <%= column_chart @yearly_sales %>
    </div>
  </div>
</div>
