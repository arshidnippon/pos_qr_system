<!DOCTYPE html>
<html>
  <head>
    <title>POS System</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application.tailwind", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "chartkick" %>
    <%= javascript_importmap_tags %>

  </head>

  <!-- Use flex layout to push footer to the bottom -->
  <body class="bg-gray-50 text-gray-900 font-sans min-h-screen flex flex-col">

    <!-- Full height content wrapper -->
    <div class="flex-1 flex flex-col">
      
    <!-- Header / Nav -->
    <header class="bg-white shadow my-[10px]">
      <div class="max-w-7xl mx-auto px-4 py-6 flex justify-between items-center min-h-[64px]">
        <h1 class="text-2xl font-bold text-blue-600">POS System</h1>
        <div>
          <% if user_signed_in? %>
            <span class="mr-3 text-sm text-gray-700"><%= current_user.email %></span>
            <%= link_to "Logout", destroy_user_session_path, method: :delete, class: "px-4 py-2 bg-red-500 text-white rounded hover:bg-red-600 text-sm" %>
          <% else %>
            <%= link_to "Login", new_user_session_path, class: "px-4 py-2 bg-blue-500 text-white rounded hover:bg-blue-600 text-sm" %>
          <% end %>
        </div>
      </div>
    </header>
    

      <!-- Main Content -->
      <main class="flex-1 py-8 px-4 sm:px-6 lg:px-8 max-w-7xl mx-auto w-full">
        <%= yield %>
      </main>
    </div>

    <!-- Sticky Footer -->
    <footer class="bg-white text-center text-sm text-gray-500 py-4 border-t">
      &copy; <%= Time.now.year %> POS System. All rights reserved.
    </footer>
    
  </body>
</html>
